---
name: better-auth-patterns
description: Implement Better Auth (v1.4+) with TanStack Start + Drizzle ORM + Neon PostgreSQL. Use when the user asks to set up authentication, add auth to a project, or when working with Better Auth, login, register, session management, or auth middleware.
metadata:
  author: user
  version: "1.0.0"
  stack: better-auth@^1.4 + tanstack-start@^1.132 + drizzle-orm@^0.45 + neon-postgres
---

# Better Auth Patterns for TanStack Start

Patrones extraídos de un proyecto real con Better Auth + TanStack Start + Drizzle ORM + Neon PostgreSQL.

Lee el archivo `AGENTS.md` en este mismo directorio para ver los patrones completos de implementación.
